#!/usr/bin/env ruby
require 'gameboard'

class ShowOff
  include Gameboard
  def initialize
    @board = Board.new(height: 8, width: 10, cells: "O")
    p @board.horizontal
    Render.board(@board.horizontal)
  end
end

ShowOff.new